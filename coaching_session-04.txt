IA: Nice, this is a solid example—there’s a lot going on here 👍  
You’re:  

Validating input (validationResult)  
Handling errors with flash messages  
Pulling data from both req.params and req.body  
Calling your model (updateCategory)  
Redirecting based on outcome  
That’s exactly the kind of real-world flow we want.  

Let’s zoom in a bit on your understanding:  

👉 In your own words, what’s the difference between req.params.categoryId and req.body.c_name, and why do you need both in this function?  

Sebas: the req.params gets the ID specifically and the req.body gets whatever I want to be the body, in this case its only the name  

IA: Yep—you’re on the right track 👍  
You correctly see that:  

req.params.categoryId → comes from the URL  
req.body.c_name → comes from the form input  

Let me refine your understanding just a bit so it’s really solid:  

req.params is specifically for route parameters (like /edit-category/:categoryId) → it identifies which resource you’re working with.  
req.body contains the data submitted by the form → it represents what changes you want to make.  

So in your case:  
categoryId = which category to update  
c_name = what to update it to  

That separation is actually super important in backend design.  

Now let’s go one level deeper:  

👉 Why do you think you redirect back to /edit-category/:id when there are validation errors instead of rendering the view directly?  

Sebas: It prevents duplication  

IA: Good instinct—you’re circling around an important concept, but “prevents duplication” isn’t quite the full picture here.  

What your code is doing is part of a common pattern called Post/Redirect/Get (PRG):  

The form submits (POST)  
If there are errors → you redirect  
Then the browser makes a fresh GET request to show the form again  

This helps avoid issues like:  
Resubmitting the form if the user refreshes  
Weird browser warnings about “resend form data”  

But there’s a tradeoff: when you redirect, you lose the current request state (like form input), which is why you’re using req.flash() to persist messages across requests.  

👉 So here’s the key question: if you redirect after validation errors, how would you preserve the user’s previously entered input so the form isn’t empty when it reloads?  

Sebas: Im not sure because I didnt did that this week, but maybe I would use server session data or local storage  

IA: That’s a really good instinct—you’re thinking in the right direction 👍  

Between the two options you mentioned:  

Local storage → usually handled on the client (JS in the browser)  
Sessions (server-side) → much more aligned with what you're already doing using req.flash()  

So yes—sessions would be the correct approach here in a server-rendered app like yours.  

A common pattern is:  

Store the submitted form data in the session (or flash)  
Redirect  
On the GET request, pull that data and repopulate the form inputs  

Now let’s smoothly move into Topic 2: Forms for Updates, since this connects directly.  

👉 Can you paste your EJS form snippet for editing a category (the one where you pre-fill the input with existing data)?  

Sebas:  
<%- include('partials/header') %>  
<main>  
    <h1><%= title %></h1>  

    <form action="/edit-category/<%= category.categories_id %>" method="POST">  
        <div class="form-group">  
            <label for="c_name">Category Name</label>  
            <input  
                type="text"  
                id="c_name"  
                name="c_name"  
                maxlength="100"  
                required  
                value="<%= category.c_name %>"  
            />  
        </div>  

        <button type="submit">Update Category</button>  
    </form>  
</main>  
<%- include('partials/footer') %>  

IA: Nice—this is a clean and correct update form 👍 You’re properly pre-filling the input using:  

value="<%= category.c_name %>"  

That’s exactly what allows the user to see the current value and edit it, which is key for update forms.  

Also good job tying the form action to the specific categoryId—that connects directly with your controller using req.params.  

Let’s think a bit deeper about real-world behavior:  

👉 If validation fails and you redirect back to this form, what value will show in the input—the original category.c_name from the database, or the user’s attempted (invalid) input—and why?  

Sebas: In my code, the original c_name, but I guess If I use server session It would be the users attempted input  

IA: Exactly—that’s a very solid understanding 👍  

Right now:  

You redirect  
The GET route fetches the category again from the database  
So the form shows the original c_name  

But if you store the user’s attempted input (e.g., in session or flash), then:  

You can pass that back to the view  
And override category.c_name in the input  
So the user doesn’t lose what they typed  

That’s a really important UX improvement.  

Let’s move into Topic 3: Sessions and Flash Messages, since you’re already using them here.  

👉 Can you paste a code snippet from your app where you configure or use sessions/flash messages (like middleware setup or how you display them in your views)?  

Sebas:  

/**  
 * Flash Message Middleware  
 *   
 * Provides temporary message storage that survives redirects but is consumed on render.  
 * Messages are stored in the session and organized by type (success, error, warning, info).  
 *   
 * Usage in controllers:  
 *   req.flash('success', 'Message text')  // Store a message  
 *   req.flash('error')                    // Get all error messages  
 *   req.flash()                           // Get all messages (all types)  
 /  

/*  
 * Initialize flash message storage and provide access methods  
 /  
const flashMiddleware = (req, res, next) => {  
    /*  
     * The flash function handles both setting and getting messages  
     * - Called with 2 args (type, message): stores a new message  
     * - Called with 1 arg (type): retrieves and clears messages of that type  
     * - Called with 0 args: retrieves and clears all messages  
     /  
    req.flash = function(type, message) {  
        // Initialize flash storage if it doesn't exist  
        if (!req.session.flash) {  
            req.session.flash = {  
                success: [],  
                error: [],  
                warning: [],  
                info: []  
            };  
        }  

        // SETTING: Two arguments means we're storing a new message  
        if (type && message) {  
            // Ensure this message type's array exists  
            if (!req.session.flash[type]) {  
                req.session.flash[type] = [];  
            }  
            // Add the message to the appropriate type array  
            req.session.flash[type].push(message);  
            return;  
        }  

        // GETTING ONE TYPE: One argument means retrieve messages of that type  
        if (type && !message) {  
            const messages = req.session.flash[type] || [];  
            // Clear this type's messages after retrieving  
            req.session.flash[type] = [];  
            return messages;  
        }  

        // GETTING ALL: No arguments means retrieve all message types  
        const allMessages = req.session.flash || {  
            success: [],  
            error: [],  
            warning: [],  
            info: []  
        };  

        // Clear all flash messages after retrieving  
        req.session.flash = {  
            success: [],  
            error: [],  
            warning: [],  
            info: []  
        };  

        return allMessages;  
    };  

    next();  
}  

/*  
 * Make flash function available to all templates via res.locals  
 * This middleware must run AFTER flashMiddleware  
 /  
const flashLocals = (req, res, next) => {  
    // Attach the flash function to res.locals so templates can access it  
    // The function is NOT called here, just made available  
    // Messages are only consumed when a template calls flash()  
    res.locals.flash = req.flash;  
    next();  
}  

/*  
 * Combined flash middleware that runs both functions in the correct order  
 * Import and use this as a single middleware function in your application  
 */  
const flash = (req, res, next) => {  
    flashMiddleware(req, res, () => {  
        flashLocals(req, res, next);  
    });  
};  

export default flash;  

IA: This is really well done—seriously 👍 you didn’t just use flash messages, you implemented your own system, which shows a strong understanding of how sessions work under the hood.  

You clearly understand that:  

Flash messages live in req.session  
They persist across redirects  
They get consumed (cleared) after being read  

That’s exactly the intended behavior.  

Let’s zoom in on one key concept to make sure it’s fully solid:  

👉 Why do you think flash messages are cleared right after being retrieved instead of staying in the session?  

Sebas: Because thats the way flash work, its a message just to tell you whats wrong or what is required for the form  

IA: Yeah—that’s the idea 👍 you’re describing the purpose, which is good.  

Let me sharpen it just a bit so it’s crystal clear:  

Flash messages are cleared after being retrieved because they’re meant to be temporary, one-time feedback tied to a single request cycle (usually after a redirect). If they weren’t cleared:  

The same error/success message would keep showing on every page refresh  
That would confuse users (“wait, did it fail again?”)  

So clearing them ensures:  
👉 the message shows once, exactly when it’s relevant  

Let’s move to our final area: Topic 4 – Server-side Validation (this connects directly to your earlier controller).  

👉 Can you paste the validation rules you used (e.g., with express-validator or similar) for something like c_name, and explain what each rule is enforcing?  

Sebas:  
import { body, validationResult } from 'express-validator';  

const projectValidation = [  
    body('title')  
        .trim()  
        .notEmpty().withMessage('Title is required')  
        .isLength({ min: 3, max: 200 }).withMessage('Title must be between 3 and 200 characters'),  
    body('description')  
        .trim()  
        .notEmpty().withMessage('Description is required')  
        .isLength({ max: 1000 }).withMessage('Description must be less than 1000 characters'),  
    body('location')  
        .trim()  
        .notEmpty().withMessage('Location is required')  
        .isLength({ max: 200 }).withMessage('Location must be less than 200 characters'),  
    body('date')  
        .notEmpty().withMessage('Date is required')  
        .isISO8601().withMessage('Date must be a valid date format'),  
    body('organizationId')  
        .notEmpty().withMessage('Organization is required')  
        .isInt().withMessage('Organization must be a valid integer')  
];  

I used express validator  

IA: Nice—this is a strong validation setup 👍 You’re using express-validator exactly how it’s intended.  

You’re enforcing:  

.trim() → cleans input (avoids whitespace-only values)  
.notEmpty() → required fields  
.isLength() → prevents too short/too long input  
.isISO8601() → ensures a valid date format  
.isInt() → enforces correct data type for IDs  

That’s solid server-side protection and shows good attention to data integrity.  

Let’s go one level deeper on why this matters:  

👉 If you already have required and constraints in your HTML form, why is it still necessary to validate all of this again on the server?  

Sebas: the client side validation can be easily bypass, while server side validation is the real validator that keeps my app web secure  

IA: Exactly—that’s the key idea 💯  

Client-side validation is mainly for user experience (faster feedback), but it can be bypassed easily (e.g., disabling JS, sending requests with Postman, modifying requests manually).  

Server-side validation is the source of truth that actually protects your application:  

Prevents invalid or malicious data from reaching your database  
Enforces data integrity regardless of how the request was made  
Acts as a security layer, not just convenience  

You’ve got a solid grasp of that.  

We’ve now covered all four topics:  

Form handling ✅  
Update forms (EJS) ✅  
Sessions & flash messages ✅  
Server-side validation ✅ 
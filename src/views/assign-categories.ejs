<%- include('partials/header') %>
<main>
    <h1><%= projectDetails.title %></h1>

    <h2>Assign Categories</h2>
 <form action="/assign-categories/<%= projectId %>" method="POST">

    <div class="form-group">
      <% categories.forEach(category => { %>
        <div>
          <input
            type="checkbox"
            id="category-<%= category.categories_id %>"
            name="categoryIds"
            value="<%= category.categories_id %>"
            <%= assignedCategories.some(ac => ac.categories_id === category.categories_id) ? 'checked' : '' %>
          />
          <label for="category-<%= category.categories_id %>">
            <%= category.c_name %>
          </label>
        </div>
      <% }); %>
    </div>

    <button type="submit">Update Categories</button>

</main>
<%- include('partials/footer') %>
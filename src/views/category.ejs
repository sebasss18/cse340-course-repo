<%- include('partials/header') %>

<main>
    <h1><%= category.c_name %></h1>
    <% projects.forEach(project => { %>
        <li>
            <a href="/project/<%= project.project_id %>">
                <%= project.title%>
            </a>
        </li>
    <% }); %>
    <a href="/edit-category/<%= category.categories_id %>">Edit Category</a>
</main>

<%- include('partials/footer') %>